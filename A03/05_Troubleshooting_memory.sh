#Working on ubuntu

free

free -h

cat /proc/meminfo

sar -r

top

#f
#Arrow Down To MEM
#s
#q

We can view memory utilization as a whole, that data come from the procfs. To view collected
information, we have sar -r.

To show processes sorted by most memory used we can use top and filter on memory.